# Frontend production env — copy to .env.production and fill in your API URL.
# Vite automatically loads .env.production during `npm run build`.
# .env.production is gitignored; commit only this example file.

VITE_GRAPHQL_URL=https://api.yourdomain.com/graphql
